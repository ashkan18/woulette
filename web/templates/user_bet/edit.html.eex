<h2>Edit user bet</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_bet_path(@conn, :update, @user_bet) %>

<%= link "Back", to: user_bet_path(@conn, :index) %>
